// popup.js
// This file runs whenever the user clicks on the extension icon in the toolbar.

document.addEventListener("DOMContentLoaded", () => {
    // Currently, there's no complex functionality required.
    // In the future, you could add things like pausing the extension,
    // viewing stats, setting options, etc.
    
    console.log("Malicious URL Detector popup opened successfully.");
});
